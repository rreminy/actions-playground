name: Test secret

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - if: ${{ false }}
        name: Test this output
        run: |
          ls /usr/sbin
        env:
          SECRET: ${{ secrets.SECRET }}
